# Google Tag Manager container ID (e.g., GTM-XXXXXXX)
# Set this as a GitHub Actions repository variable (Settings → Secrets and variables → Actions → Variables)
# For local development, copy this file to .env and fill in the value.
PUBLIC_GTM_ID=
