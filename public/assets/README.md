# Media Assets

This directory contains multimedia resources for the site:

- `logo/` — Logo files (SVG, PNG, etc.)
- `banners/` — Hero banner and page banner images
- `ads/` — Advertisement and promotional images
- `images/` — General-purpose images

Place your assets in the appropriate subdirectory. They are served from `/assets/` at the site root.
